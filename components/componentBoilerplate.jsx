import { styled } from '@stitches/react'

export const element = styled(element, {
	variants: {
		color: {
			light: {
				//css for light theme
			},
			dark: {
				//css for dark theme
			}
		}
	},
	defaultVariants: {
		color: 'light'
	},
	//css
})