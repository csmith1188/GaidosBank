import { styled } from '@stitches/react'

export const element = styled(element, {
	variants: {
		theme: {
			light: {
				//css for light theme
			},
			dark: {
				//css for dark theme
			}
		}
	},
	defaultVariants: {
		theme: 'light'
	},
	//css
})